<Window x:Class="RCVL.IntegrationTests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RCVL.IntegrationTests"
        xmlns:rcvl="clr-namespace:RCVL;assembly=RapidCellularViewportsLibrary"
        mc:Ignorable="d"
        Title="MainWindow" Width="525" Height="350">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <rcvl:CellularViewport
            CellularData="{Binding Path=LayeredData}" Name="cellularViewport"
            Grid.Row="0" Grid.Column="0"/>

        <TextBlock
            Text="{Binding ElementName=cellularViewport, Path=CellularData}"
            Grid.Row="1" Grid.Column="0"/>
    </Grid>
</Window>